import { ComingSoon } from '@/components/coming-soon';

export default function LoginPage() {
  return (
    <ComingSoon
      title="Sign in"
      phase="Phase 0 · Milestone 3"
      blurb="Magic-link sign-in arrives once Auth.js v5 is wired. Google and Apple OAuth land in the same milestone; passkeys for contractors follow."
    />
  );
}
